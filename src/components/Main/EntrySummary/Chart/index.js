import React from 'react';
import {Text, StyleSheet} from 'react-native';

const Chart = () => {
  return <Text style={styles.Chart}>Gráfico 01</Text>;
};

const styles = StyleSheet.create({
  Chart: {},
});

export default Chart;
